apply plugin: 'java'

jar {
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    manifest {
        attributes 'Main-Class': 'com.roger.GetExample'
    }
}

repositories {
    jcenter()
}

dependencies {
    compile 'com.squareup.okhttp:okhttp:2.4.0'
}
